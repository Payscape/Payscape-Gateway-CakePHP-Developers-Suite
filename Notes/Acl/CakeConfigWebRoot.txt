
Thought it might be a good idea to actually have a full 
set up, so we installed the complete app from a fresh download 
of Cake 2.2.2

here is the configuration vacationfish uses that works: 


/**
 * The full path to the directory which holds "app", WITHOUT a trailing DS.
 *
 */
if (!defined('ROOT')) {
    // PRODUCTION SERVER
    //define('ROOT', dirname(dirname(dirname(__FILE__))));
    // XAMPP DEV SERVER
    define('ROOT', 'C:' . DS . 'xampp' . DS . 'htdocs' . DS . 'vacationfish.localdomain');  
}

/**
 * The actual directory name for the "app".
 *
 */
if (!defined('APP_DIR')) {
    // PRODUCTION SERVER
    //define('APP_DIR', basename(dirname(dirname(__FILE__))));
    // XAMPP DEV SERVER
    define('APP_DIR', 'app');
}

/**
 * The absolute path to the "cake" directory, WITHOUT a trailing DS.
 *
 * Un-comment this line to specify a fixed path to CakePHP.
 * This should point at the directory containing `Cake`.
 *
 * For ease of development CakePHP uses PHP's include_path.  If you
 * cannot modify your include_path set this value.
 *
 * Leaving this constant undefined will result in it being defined in Cake/bootstrap.php
 */
// PRODUCTION SERVER
//define('CAKE_CORE_INCLUDE_PATH', '/usr/share/php/cake/cake_2.2.2/lib');

// XAMPP DEV SERVER
define('CAKE_CORE_INCLUDE_PATH', 'C:' . DS . 'xampp' . DS . 'htdocs' . DS .'vacationfish.localdomain' . DS . 'cakephp-2.2.2' . DS . 'lib');
//                                C:    \      xampp    \      htdocs  \    vactionfish.localdomain\cakephp-2.2.2\lib\Cake\bootstrap.php
//example from another Cake PHP web 
//define('ROOT', 'C:' . DS . 'xampp' . DS . 'htdocs' . DS . 'vacationfish.localdomain' . DS . 'cakephp-2.2.2');
/**