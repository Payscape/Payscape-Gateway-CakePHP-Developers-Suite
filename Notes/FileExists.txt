
	our carousel produces errors if images are missing. 
	so we want to check to see if the image exists before 
	including it in the carousel. 
	
	like this: 
	
	    $image_path = WWW_ROOT . '/img/files/' . $image['property_id'] . '/images/' . $image['image'];
	  
if(is_file($imagepath)==TRUE){
	// do the stuff to display the image
	}	  