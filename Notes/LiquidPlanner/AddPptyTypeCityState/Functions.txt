
1 we need a function to get the address 
from the Address field 

we do not need this for the Listings results, 
but instead for other places where the address is displayed. 




	ADDRESS
	
	$address = $pr['address'];

if in_str($address, ", "){
$address_array = explode(", ", $address, 1);
	$address_display = $address_array[0];
} else {
	$address_display = $property['address'];
}

	$geonames = "SELECT asciiname, state_name, country_name FROM geonames WHERE id = $geoname_id";

	CITY
		$city = $geonames['asciiname'];

	STATE
		$state = $property['AdminCode']['nameAscii']
		
	COUNTRY
		$country = $geonames['country_name'];

$state = $property['AdminCode']['nameAscii'];


4212992 	3571

ok we can get city, state and country right out of geonames:
	ascii_name = North Decatur
	country = US
	state_name = Georgia
	
	

"SELECT ascii_name,  "
