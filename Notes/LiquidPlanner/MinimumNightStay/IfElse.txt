There is no if / else statement in jQuery, 
it is a JavaScript function. 

	so we should be able to 
	1 set a variable for the minimum night stay
	2 set a variable returned by the query to the controller
	3 popup the Calendar if the values are incorrect
	4 display a popover for the End Date field in the pop up. 
	

var	a = 5,
var	b = 10;

if(a > b) {
	$("body").html("<p>A is larger than B</p>");
}
else if(a == b) {
	$("body").html("<p>A is equal to B</p>");
}
else {
	$("body").html("<p>B is larger than A</p>");
}

	so, 
	var minimum_night_stay = <?php echo $property['Property']['minimum_nights'];

$(document).ready(function(){ 

	var n = $("#example div").length;
	// here we are setting a jQuery value we will compare in JavaScript

	if (n < 2) {
		$("body").css("background", "green");
	} 
	else {
		$("body").css("background", "orange");
	}

});