this is simpler than this. 

	we are calculating the number of nights when we get the rate
	so, $check_nights = $variable['nights']
		
	<?php	
	if ($check_nights < $minimum_nights) {
	?>	
		<script>
			$("#minimum_nights").show();
		</script>
	<?php } ?>
	
	
	<script>
	jQuery function(){
		$("#minimum_nights").show();	
	});
	
	</script>

we should be able to use jQuery Validation Engine 
for our date pickers 

what would be really clever would be to have the widget
display the error message if the date is not > than 
minimum night stay date. 

so: 
	$start_date = $this->MinimumNight['start_date'];
	
	$minimum_night_date = $start_date + $minium_nights['minimum_nights'];
	
	if (! $date['value'] > $minimum_night_date ){
			//
	}
	

				validate[custom[date],future[NOW]]


	var minimum_night_stay = <?php echo $property['Property']['minimum_night_stay'];

 if( ! nights == minimum_night_stay
 
                        <?php echo $this->Html->scriptblock('
                            $("#check_it").on("click", function(evt) {
                                if ( ! $("#sc3").is(":visible")) {
                                    loadTabs1("calendars");
                                    return false;
                                }
                                else if (("" == $("#start_date").val( )) || ("" == $("#end_date").val( ))) {
                                    alert("Please select Check-in and Check-out dates");
                                    return false;
                                }
                            });
                        '); ?>