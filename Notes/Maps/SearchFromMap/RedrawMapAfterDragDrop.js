

	/*
	 * call our function to get our new properties object
	 * when the map has been moved. 
	 * 
	 * if we do this correctly, we want to unset the destination
	 * then we want to hit a different function in the model
	 * 
	 * find_map() or something to that effect, that uses our new 
	 * magic formula. 
	 * 
	 * $properties = $this->Search->getMap($conditions, $lat, $lng);
	 * 
	 */*/