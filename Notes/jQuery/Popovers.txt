	<div id="foto-flag" rel="popover" class="photo-flag" style="width:100px;" data-content="This is your first image. Make it speak. This image appears in search results and gives guests their first impression. You can reorder your photos at anytime to change the main image." data-original-title="Image sells.">Main img</div>
	
	
	    $(document).ready(function() {
        $('.icon-star').popover('show');
         $(this).popover('show');
    });
    
    now we want to display a popover styled div
    if the property has a minimum night stay, 
    and that minimum night stay is greater 
    than the number of nights. 
    
    so lets do something like this:
    
   	the price calculation will have to fire 
   	before we can run this, 
   	
   	put the value for minimum_night_stay_val
   	into the input id="minimum_night_stay" name="minimum_night_stay"
   	
   	hold on: get the value for minimum night stay
   	when the view loads with php and use it 
   	to populate the hidden_minimum_night stay field
   	then put the value for "days" that our Ajax returns
   	then do the comparison. 
   	
   	
       function(data){
          $('#days').val(data.days);
       }
                          )  	
     minimum nights stay = 6
     if minimum nights stay > nights selected, 
     we want to interrupt with the alert. 
     
    if(parseInt($"#minimum_night_stay").val(), 10) > parseInt($(".calc_nights").val(), 10)){
    		
    		$("#minimum_night_popover").show();
    		// make the submit button disappear
    		$("#submitter").hide();
    }
    
    if (parseInt($(".count_1 .percentage").html(), 10) > parseInt($(".count_2 .percentage").html(), 10))
				