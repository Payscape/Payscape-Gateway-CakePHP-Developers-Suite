
	to set form field value:
	$("#input3").val(<?php echo $value3; ?>);
	
	then we can retrieve that value:
	var value3 = $("#input3").val();
	
	quite simple really. worked for hours attempting to do the same 
	thing with a div element, never did work at all. 
	
	