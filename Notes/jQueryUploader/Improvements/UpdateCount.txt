
Air has a feature in their file uploader, 
looks like when the uploader has concluded, it fires 
a POST to the controller to get the new count for the file upload count. 

so, that would indicate that element is generated by jQuery 
rather than Ruby or PHP. 

shouldn't be too hard to get this. 

	var photocount = 0;
	
	.post(
	
	we will be on the PropertyImages controller, 
	so let's use a function on there to do this. 
	
	url: propertyimages/count
	
	property_id
	
	take a look at the ajax delete function to see how to throw 
	the property id at the controller. 
	
	
	function() {
	        $.post(ROOT_URL + "propertyiamges/count", {
            pid: <?php echo $property['id'] ?>;
        }, function (data) {
            $("#image_count").html(data);
        });
        
        //
         $.post("<?php echo base_url(); ?>blade/bore_images", {
                    dia: diameter,
                    thi: thickness,
                    bor: bore
                }, function (data) {
       
                    $("#bore_message").html(data);
                });
        //
        
    $.post(ROOT_URL + "propertyimages/count", {
                
                   property_id: pid;
                }, function (data) {
       
                    $("#image_count_edit").html(data);
                    $("#image_count_view").html(data);
                    $("#image_count_delete).html(data);
                });  
                
                
          <script>

// almost working, needed to add return false
$(function(){
var pid = <?php echo $id; ?>;

$.post(ROOT_URL + "propertyimages/count", 
		{ property_id: pid }, 

		function (data) {
     $("#image_count_edit").html(data);
     $("#image_count_view").html(data);
     $("#image_count_delete").html(data);
 });     

});
</script>         